import Carousel from "./Carousel";
import Hero from "./Hero";

const Content = (): JSX.Element => {
  return (
    <section>
      <Hero />
      <Carousel />
    </section>
  );
};

export default Content;
